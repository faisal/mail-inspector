<h1>Listing mailboxes</h1>

<table>
  <tr>
    <th>URL</th>
    <th>Message count</th>
  </tr>

<% @mailboxes.each do |mailbox| %>

  <tr>
    <td><%= mailbox.url %></td><td><%= mailbox.messages.count%></td>
  </tr>
<% end %>
</table>

<br />
